# دليل تشغيل تطبيق تكتك (من الموبايل فقط)

## الفكرة العامة
ما نقدر نبني APK على الموبايل مباشرة. الحل: نرفع الكود على GitHub، وخدمة
GitHub Actions (مجانية) تبنيه تلقائياً بالسحابة، وتنزل أنت ملف الـ APK الجاهز.

---

## الخطوة 1: إنشاء مشروع Firebase (5 دقائق)
1. افتح https://console.firebase.google.com من متصفح الموبايل
2. سجل دخول بحساب Google
3. "إضافة مشروع" -> اكتب اسم المشروع (tuktuk-app مثلاً)
4. من القائمة الجانبية: **Authentication** -> فعّل طريقة "Email/Password"
5. من القائمة الجانبية: **Firestore Database** -> إنشاء قاعدة بيانات -> ابدأ بوضع
   "test mode" مؤقتاً (رح نستبدلها بملف firestore.rules المرفق لاحقاً)
6. اضغط على أيقونة Android لإضافة تطبيق Android:
   - Package name: `com.example.tuktuk_app`
   - نزّل ملف `google-services.json` (رح تحتاجه بالخطوة 3)

## الخطوة 2: إنشاء حساب GitHub ورفع المشروع
1. سجل حساب مجاني على https://github.com (من الموبايل)
2. أنشئ repository جديد اسمه `tuktuk-app` (خليه Public أو Private، الاثنين يشتغلون)
3. من تطبيق GitHub على الموبايل أو الموقع، ارفع (Upload files) **كل الملفات
   والمجلدات** الموجودة بهذا المشروع بنفس الهيكلية

## الخطوة 3: ربط Firebase بالمشروع
1. ضع ملف `google-services.json` (اللي نزّلته بالخطوة 1) داخل مجلد `android/app/`
   في الـ repository
2. عدّل ملف `lib/main.dart` — سطر `Firebase.initializeApp()` يشتغل تلقائياً
   لو ملف google-services.json موجود بمكانه الصحيح

## الخطوة 4: تفعيل البناء التلقائي (GitHub Actions)
1. بمجرد ما ترفع الملفات (وفيها مجلد `.github/workflows/build_apk.yml` المرفق)،
   روح لتبويب **Actions** بصفحة الـ repository
2. رح تلقى عملية بناء (build) بدأت تلقائياً — انتظر 5-10 دقائق
3. لما تخلص وتصير علامة ✅ خضراء، اضغط عليها -> انزل لتحت -> بتلقى
   **Artifacts** -> حمّل `tuktuk-app-release` -> هذا ملف الـ APK جاهز!
4. افتح الملف المضغوط، ثبّت الـ APK على جوالك (لازم تفعّل "السماح بمصادر غير معروفة")

## ملاحظة مهمة عن هيكلية مشروع Flutter الكاملة
الملفات اللي جهزتها لك (لب التطبيق: الشاشات، الخدمات، المنطق) موجودة كاملة.
لكن مشروع Flutter الكامل يحتاج أيضاً مجلدات `android/` و `ios/` القياسية
اللي تتولد تلقائياً عند إنشاء مشروع Flutter جديد. أسهل طريقة:

### إذا حصلت على وصول مؤقت لأي كمبيوتر (مقهى إنترنت، صديق، مختبر جامعة):
```
flutter create tuktuk_app
```
هذا يولّد كل مجلدات android/ios القياسية، وبعدها تنسخ فوقها ملفات lib/ و
pubspec.yaml و firestore.rules و .github/ اللي جهزتها لك هنا.

### إذا ما عندك وصول لأي كمبيوتر أبداً:
تواصل وياي وبجهزلك أيضاً محتوى مجلد android/ الأساسي (build.gradle,
AndroidManifest.xml, إلخ) كملفات نص تقدر ترفعها يدوياً — بس هذا الجزء
أطول شوي فخليني أعرف إذا تحتاجه.

---

## الخطوات التالية بعد ما يشتغل الأساس
- إضافة خريطة حقيقية (Google Maps) لعرض موقع الكابتن لحظياً للزبون
- نظام تقييم بعد كل رحلة
- إشعارات push (Firebase Cloud Messaging)
- طرق دفع إلكترونية

أخبرني إذا صارت عندك مشكلة بأي خطوة وبساعدك فيها بالتفصيل.
